/**
 * @class
 * @param {Object} opts
 * @param {*} opts.feed
 */
function rpc({ feed }) {
  if (!(this instanceof rpc)) return new rpc({ feed });

  const handler = 

  feed.registerExtension('rpc', handler);
}
